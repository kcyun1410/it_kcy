9-1

Vue Router 도 외부 라이브러리이기 때문에 다운로드가 필요
Terminal 윈도우에
npm i vue-router 입력.

vue랑 vue router 연결. vue router랑 conponent 연결

router는 실제화면은 1개지만 화면이 변경된 것 처럼 느끼게 해줌

-----------------------------------------------------------------------------------------------

9-2
주소에 #/주소명 이면 #(해시)라우터
-좋은점 : 새로고침을 했을 때에도 동작을 함
브라우저에서만 쓰임
실무에서는 #라우터 말고 /를 많이 씀
#라우터를 쓰면 검색엔진에 잘 검색이 안됨.
view 라우터는 기본적으로 #라우터

새로고침은 서버에 요청이기 때문에 에러가 날 수 밖에 없음
router.html이 처리하기 때문에 #라우터는 불러올 수 있지만 /라우터는 불가능함.

--------------------------------------------------------------------------------------------------

9-3
동적 주소.

--------------------------------------------------------------------------------------------------------

9-4
쿼리 스트링
주소를 통해서 데이터를 전달하는 방법